from django.apps import AppConfig


class SubscriptionsConfig(AppConfig):
    name = 'eventex.subscriptions'
    verbose_name = 'Controle de Participantes' #nome que aparecerá no módulo administrativo